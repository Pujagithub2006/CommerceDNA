<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.commercedna</groupId>
        <artifactId>commercedna-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>commercedna-negotiation</artifactId>
    <packaging>jar</packaging>

    <name>CommerceDNA Negotiation &amp; Intent Settlement</name>
    <description>Agentic proposal negotiation engine, cryptographic counter-signing, and state machines</description>

    <dependencies>
        <!-- Internal CommerceDNA Modules -->
        <dependency>
            <groupId>io.commercedna</groupId>
            <artifactId>commercedna-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.commercedna</groupId>
            <artifactId>commercedna-identity</artifactId>
        </dependency>
        <dependency>
            <groupId>io.commercedna</groupId>
            <artifactId>commercedna-catalog</artifactId>
        </dependency>

        <!-- Spring Data JPA & Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- LLM Integration -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
            <version>1.0.0-M4</version>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>

